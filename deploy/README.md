# Deployment

`deploy/` is reserved for reproducible server deployment material:

- Caddy/reverse-proxy configuration;
- systemd unit templates;
- bootstrap and rebuild scripts;
- export/import orchestration;
- VPS runbooks.

No secrets belong here.
